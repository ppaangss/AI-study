# AI-study 위키 규칙

매일 AI에게 배운 것을 추적하는 **순수 인덱스** 위키. 공부 내용 본문은 블로그에 있고, 여기는 주제·한줄요약·블로그 링크만 담는다.

## 구조와 역할

| 경로 | 역할 | 누가 쓰나 |
| --- | --- | --- |
| `daily/YYYY-MM-DD.md` | 그날 공부한 주제 목록 | **사용자가 직접 — 유일한 기입처** |
| `topics/<카테고리>/<주제>.md` | 주제별 공부 이력 테이블 | `/sync` 스킬만 (직접 편집 금지) |
| `README.md`의 SYNC 마커 사이 | 주제별 인덱스 + 최근 공부 | `/sync` 스킬만 |
| `raw/` | 공부 재료 임시보관소 (블로그 글 쓸 때 참고) | 자유 — 스킬은 건드리지 않음 |

**daily가 원본, topics와 README 인덱스는 파생물이다.** 파생물을 손으로 고치지 않는다 — 다음 sync 때 덮어써진다.

## daily 항목 형식

`daily/_template.md` 참조. 항목 하나는 `## 제목` + `카테고리:`(필수, `대분류/주제` 형식) + `요약:`(필수) + `블로그:`(선택, 글 쓴 후 추가).

## 링크 규칙

GitHub 웹에서도 동작해야 하므로 Obsidian `[[위키링크]]`가 아닌 **일반 마크다운 상대 링크**만 쓴다.

## sync

`/sync` 호출 시에만 동작. `.claude/skills/sync/SKILL.md` 참조. 블로그 링크를 daily에 나중에 추가해도 재실행하면 topics에 반영된다(멱등).
